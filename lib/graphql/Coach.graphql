query Coach($username: String!) {
  coach(username: $username) {
    _id
    name
    title
    description
    picture
    skills {
      skill
    }
  }
  currentUser {
    _id
    studentId
  }
  canRequestCoaching(coachUsername: $username)
}
